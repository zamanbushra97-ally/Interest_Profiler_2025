# --- Core web framework ---
fastapi>=0.110
uvicorn[standard]>=0.23

# --- Data handling ---
pandas>=2.0
numpy>=1.24
openpyxl>=3.1        # read .xlsx for RIASEC items

# --- Pydantic (FastAPI depends on v2 now) ---
pydantic>=2.0

# --- Environment config ---
python-dotenv>=1.0

# --- Multipart (file uploads). Safe to keep even if unused now. ---
python-multipart>=0.0.9

